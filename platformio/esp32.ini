[esp32]
platform = espressif32
board = esp32dev
filesystem = littlefs
extra_build_flags =
    -DARDUINO_ARCH_ESP32

[env:esp32dev]
platform = ${esp32.platform}
board = ${esp32.board}
board_build.filesystem = ${esp32.filesystem}
build_flags =
    ${env.build_flags}
    ${esp32.extra_build_flags}