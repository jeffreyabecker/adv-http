[native]
platform = native
test_framework = unity
test_build_src = no
build_flags =
    -DUNIT_TEST

[env:native]
platform = ${native.platform}
test_framework = ${native.test_framework}
test_build_src = ${native.test_build_src}
build_flags =
    ${native.build_flags}