#pragma once

namespace HttpServerAdvanced
{
    namespace Compat
    {
        // COMPAT-004 will define the Arduino alias path and the minimal
        // non-Arduino filesystem and file-handle surfaces here.
        class File;
        class FileSystem;
    }
}