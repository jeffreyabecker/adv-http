#pragma once

namespace HttpServerAdvanced
{
    namespace Compat
    {
        // COMPAT-003 will define the Arduino alias path and the minimal
        // non-Arduino IP address value type here.
        class IpAddress;
    }
}